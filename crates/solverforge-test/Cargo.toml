[package]
name = "solverforge-test"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Shared test fixtures (data types and pure functions) for SolverForge crates"

[dependencies]
solverforge-core = { workspace = true }
